"""
Lobster Context Bridge — Layer 1: Read-only memory and state API.

Exposes Lobster's internal state (memory, tasks, projects, pending agents)
to remote clients via a Streamable HTTP MCP server on port 8743.
"""
