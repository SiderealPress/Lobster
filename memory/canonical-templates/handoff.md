# Lobster Handoff Document

*Auto-generated by nightly consolidation. Last updated: [date]*

## Identity

Lobster is an always-on AI assistant built on Claude Code. It processes messages from Telegram (and other channels) and responds to users. Lobster runs as a persistent Claude Code session on a cloud VM.

## Owner

[Owner name] -- primary user and operator. Prefers concise responses (mobile user).

## Active Projects

*(Projects will be populated by the nightly consolidation process.)*

## Key Relationships

*(People and contacts will be populated by the nightly consolidation process.)*

## Priority Stack

1. *(Priorities will be populated by the nightly consolidation process.)*

## Pending Decisions

*(Decisions will be populated by the nightly consolidation process.)*

## Recent Trajectory

*(Recent activity will be populated by the nightly consolidation process.)*

## System Notes

- **Architecture**: MCP server at `$LOBSTER_DIR/src/mcp/inbox_server.py`
- **Message flow**: Telegram -> `~/messages/inbox/` -> Lobster processes -> `~/messages/outbox/` -> Telegram
- **Tasks**: Stored in `~/messages/tasks.json`
- **Memory store**: SQLite + FTS5 at `~/lobster/memory/lobster_memory.sqlite`
- **Canonical memory**: `memory/canonical/` in the Lobster repo
- **Scheduled jobs**: In `~/lobster/scheduled-tasks/`
- **Dispatcher pattern**: Quick tasks (< 30s) handled directly, substantial tasks delegated to subagents

## Overdue Items

*(Overdue items will be populated by the nightly consolidation process.)*
